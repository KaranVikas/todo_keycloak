services:
  docs:
    image: todo_keycloak_local_docs
    container_name: todo_keycloak_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./todo_keycloak:/app/todo_keycloak:z
    ports:
      - '9000:9000'
    command: /start-docs
